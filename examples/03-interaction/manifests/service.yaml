apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: shop
spec:
  type: ClusterIP
  selector:
    app: orders          # whatever carries this label is an endpoint
  ports:
    - name: http
      port: 80
      targetPort: 8080
