<!-- Generated from examples/01-basics by `bazel run //site:sync`. Do not edit: //site:site_test fails while this file disagrees with its source, and the next sync overwrites it. -->

# basics

## [GET /api/orders](01-k8s-request.md)

API request path through a Kubernetes cluster. Strip the scenario block and this is ordinary Mermaid.

## [ship a release](02-deploy-pipeline.md)

A commit's journey from a laptop to production, and back again when the error budget says otherwise. It exercises the attributes the compiler emits and the runtime honours: scenario `speed` and `autoplay`, per-action `color`, and `ease` on the promotion hops.

